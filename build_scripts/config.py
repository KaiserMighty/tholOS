#config = 'release'
#arch = 'i686'
imageType = 'disk'
imageFS = 'fat32'
imageSize = '250m'
toolchain='/root/tholOS/toolchain'
mountMethod = 'mount' # change to 'guestfs' when not using WSL2, 'mount' will ask for sudo each compile